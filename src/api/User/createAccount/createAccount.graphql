type Mutation {
    createAccount(
        username:String!
        email:String!
        fistName:String
        lastName:String
        bio:String
        ): User!
}

type Query{
    someting:String!
}